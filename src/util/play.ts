import {Dispatch,SetStateAction} from "react"
function Play(minutes: number, seconds:number, setMinutes: Dispatch<SetStateAction<number>>, setSeconds: Dispatch<SetStateAction<number>>){
    setInterval(()=> ,1000)
}